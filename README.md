# 42-nvimevaluator
TLDR: a nvim config to be setup with only curl and git

```bash
# simply source in your shell the run.sh
source run.sh
# or
. run.sh
# and you can run 42vim from anywhere launch the editor
42vim

```
